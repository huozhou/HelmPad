package com.vibepad.keyboard.common

/**
 * Shared utilities (locale helpers, logging adapters, date/time wrappers, etc.).
 * Keep dependencies here to a minimum — nothing domain-specific should live here.
 */
internal object CommonPackageMarker
